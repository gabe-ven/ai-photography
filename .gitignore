# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / frontend
node_modules/
dist/
.vite/
*.local
*.tsbuildinfo

# Uploads / generated artifacts
backend/uploads/
backend/tmp/

# Eval harness: real photos and personal ground-truth judgments stay local.
# The harness (eval/run_eval.py) and schema (eval/ground_truth.example.json)
# ARE committed.
eval/photos/
eval/ground_truth.json

# Downloaded model weights (auto-fetched by ultralytics on first use — too
# large for git, exceeds GitHub's 100MB file limit)
*.pt

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
